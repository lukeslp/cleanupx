# test_direct_api (Code)

**File:** src/test_direct_api.py  
**Language:** Python  
**Lines:** 155  

**Description:** **: This script is a test file designed to demonstrate and verify the functionality of a simplified API for text and image processing. It includes several functions: `test_client()` to check if an API client can be created, `test_text_processing()` to process a sample markdown file, and `test_image_processing()` to handle image files or fall back to text processing if no images are found. The script sets up logging, manages file operations, and runs tests in a `main()` function, providing pass/fail results. Its purpose is to ensure the API works correctly in a controlled environment.

```python
#!/usr/bin/env python3
"""
Test script to demonstrate the simplified direct API.
This shows how to use the simplified_api module for text and image processing.
"""

import os
import sys
import json
import logging
from pathlib import Path

# Configure logging
logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s')
logger = logging.getLogger(__name__)

# Add the current directory to the path
current_dir = os.path.dirname(os.path.abspath(__file__))
sys.path.insert(0, current_dir)

# Import the simplified API functions
from cleanupx.utils.simplified_api import (
    analyze_text,
    analyze_image,
    get_client
)

def test_client():
    """Test that we can create an API client."""
    client = get_client()
    if client:
        logger.info("✅ Successfully created API client")
        return True
    else:
        logger.error("❌ Failed to create API client")
        return False

def test_text_processing():
    """Test text file processing with a samp
```

